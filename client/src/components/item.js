import React from "react";

const Item = (props) => {
  return (
    <div
      style={{
        width: `150px`,
        height: `150px`,
        backgroundColor: "white",
        display: `flex`,
        justifyContent: `center`,
        alignItems: `center`,
      }}
    >
      this is item
    </div>
  );
};

export default Item;
